name: cylon_dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - cmake>=3.17
  - pyarrow=5.0.0
  - glog=0.5.0
  - msmpi
  - cython>=0.29,<0.30
  - numpy>=1.16
  - pandas>=1.0
  - fsspec
  - setuptools>=40.0,<60.0
  # they are not needed for using pygcylon or compiling it
  - pytest
  - pytest-mpi
  - mpi4py
