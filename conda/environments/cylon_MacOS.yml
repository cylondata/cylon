name: cylon_dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - cmake>=3.20.1
  - pyarrow=8.0.0
  - glog=0.5.0
  - openmpi>=4.1.2
  - cython>=0.29,<0.30
  - numpy
  - pandas
  - fsspec
  - setuptools
  # they are not needed for using pygcylon or compiling it
  - pytest
  - pytest-mpi
  - mpi4py
